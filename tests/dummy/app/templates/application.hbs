{{outlet}}

<AlertContainer as |alert|>
	<p>{{alert.title}}</p>

	<div data-id="alertDescription">{{alert.description}}</div>
</AlertContainer>

<input type="button" id="openOk" {{on 'click' (fn this.open 'ok')}} value="Open ok">